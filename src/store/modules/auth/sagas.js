import { takeLatest, call, put, all } from 'redux-saga/effects';

export function* configuration({ payload }) {

}



export default all([
  // takeLatest('@configuration/SET_CONFIGURATION', configuration),
]);
